version: '3.8'
services:
  frontend:
    build: ./frontend
    ports:
      - "8080:8080"  # Map the port inside the container to the host
  backend:
    build: ./backend
    ports:
      - "5000:5000"

